@model IEnumerable<Naphat_6507_L005.ViewModels.Lab5ViewModels>

@foreach(var item in Model)
{
    @item.Name @item.LastName
}
<table class="table table-bordered">
    <thead>
        <tr>
            <th>ชื่อ</th>
            <th>นามสกุล</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
{
    <tr>
            <td>@item.Name</td>
            <td>@item.LastName</td>
        </tr>
}
    </tbody>
</table>